# これを .env にコピーして値を入れる:  cp .env.example .env && nano .env
# 権限を絞る:  chmod 600 .env
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxxxx/yyyyy

# --- 任意（既定値。変えたいときだけ）---
# INTERVAL_MS=45000          # チェック間隔(ms)
# REQUEST_GAP_MS=1000        # 商品間の待ち(ms)
# WATCHLIST_REFRESH_MS=300000  # watchlist再取得間隔(ms)
